@page "/favorites"
@using CarListingApp.Services.DTOs.Car
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]

<h1 class="fw-900">FAVORITED_VEHICLES</h1>

@if (favCars == null || !favCars.Any())
{
    <div class="loading-box">NO SAVED ENTRIES FOUND.</div>
}
else
{
    @foreach(var car in favCars) { 
        @* Use the heavy-card listing component here *@ 
    }
}

@code {
    private List<CarDto> favCars = new();

    protected override async Task OnInitializedAsync()
    {
        // Placeholder request logic
        /* try {
            favCars = await Http.GetFromJsonAsync<List<CarDto>>("favorites/me");
        } catch { }
        */
    }
}